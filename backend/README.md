# Hikari — Backend (placeholder)

Reserved for future API integration (session persistence, ambient audio,
study-session tracking, auth). Nothing lives here yet.

Suggested stack when this gets built out: FastAPI + Postgres, mirroring the
pattern used in the Digitale Barrierefreiheit project.
